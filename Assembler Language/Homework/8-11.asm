DATAS SEGMENT
    ;此处输入数据段代码  
DATAS ENDS

STACKS SEGMENT
    ;此处输入堆栈段代码
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX
    
    MOV	AL, 1CH
	MOV	AH, 35H			
	INT	21H
	PUSH ES
	PUSH BX
	PUSH DS
	MOV	AX, SEG SHOW_CLOCK
	MOV	DS, AX
	MOV	DX, OFFSET SHOW_CLOCK
	MOV	AL, 1CH
	MOV	AH, 25H			
	INT	21H
	POP	DS
	POP BX
	POP ES
	POP	DX
	POP	DS
	MOV	AL, 1CH
	MOV	AH, 25H			
	INT	21H

    ;此处输入代码段代码
    MOV AH,4CH
    INT 21H
CODES ENDS
    END START


