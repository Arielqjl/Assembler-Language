DATAS SEGMENT
    ;此处输入数据段代码  
DATAS ENDS

STACKS SEGMENT
    ;此处输入堆栈段代码
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
MAIN PROC FAR
    MOV AX,DATAS
    MOV DS,AX
    
START:		
	PUSH DS				
	SUB	AX, AX
	PUSH AX
	MOV	AX, DSEG
	MOV	DS, AX			
BEGIN:		
	MOV	CX, 100H		
	MOV	SI, 0
NEXT:		
	MOV	DX, 0
	MOV	AX, TABLE [SI]
COMP:		
	CMP	TABLE [SI], AX	;计算一个数的出现次数
	JNE	ADDR
	INC	DX
	ADD	SI, 2
	LOOP COMP
ADDR:		
	CMP	DX, COUNT		
	JLE	DONE
	MOV	COUNT, DX		
	MOV	DATA, AX		
DONE:		
	LOOP NEXT			
	MOV	CX, COUNT		
	MOV	AX, DATA		
	RET

    ;此处输入代码段代码
    MOV AH,4CH
    INT 21H
CODES ENDS
    END START

