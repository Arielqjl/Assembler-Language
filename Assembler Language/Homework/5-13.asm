DATAS SEGMENT
    STRING DB DUP(?)
    ;此处输入数据段代码  
DATAS ENDS

STACKS SEGMENT
    ;此处输入堆栈段代码
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX
    
    PUSH DS				
	SUB	AX, AX
	PUSH AX
	MOV	AX, DSEG
	MOV	DS, AX			

BEGIN:		
	MOV	SI, 0				
	MOV	CX, 100
REPEAT:	
	MOV	AL, STRING [SI]
	CMP	AL, 30H
	JB	GO_ON
	CMP	AL, 39H
	JA	GO_ON
	OR	CL, 20H			
	JMP	EXIT
GO_ON:	
	INC	SI
	LOOP REPEAT
	AND	CL, 0DFH		
EXIT:
	RET

    ;此处输入代码段代码
    MOV AH,4CH
    INT 21H
CODES ENDS
    END START

